import 'package:flutter/material.dart';

class AppColors {
  // 고향마켓 브랜드 컬러 - 자연/농촌 감성
  static const Color primary = Color(0xFF2E7D32);       // 짙은 초록 (농촌)
  static const Color primaryLight = Color(0xFF60AD5E);   // 밝은 초록
  static const Color primaryDark = Color(0xFF005005);    // 어두운 초록
  static const Color accent = Color(0xFFFF8F00);         // 따뜻한 주황 (수확)
  static const Color accentLight = Color(0xFFFFBF47);    // 밝은 주황

  // 배경
  static const Color background = Color(0xFFF5F5F0);    // 따뜻한 아이보리
  static const Color surface = Colors.white;
  static const Color cardBg = Colors.white;

  // 텍스트
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // 상태
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA726);

  // 카테고리 태그
  static const Color tagVegetable = Color(0xFF66BB6A);   // 채소
  static const Color tagFruit = Color(0xFFEF5350);       // 과일
  static const Color tagSeafood = Color(0xFF42A5F5);     // 수산물
  static const Color tagGrain = Color(0xFFFFCA28);       // 곡물
  static const Color tagProcessed = Color(0xFFAB47BC);   // 가공식품
  static const Color tagSalt = Color(0xFF78909C);        // 천일염/소금
}
